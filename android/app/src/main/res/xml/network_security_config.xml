<?xml version="1.0" encoding="utf-8"?>
<!--
  Public OSS template. By default cleartext traffic is forbidden — users
  MUST use wss:// for their server. If you really need cleartext for a
  LAN dev setup, add your own <domain> entries here and rebuild.

  Example for emulator + local LAN dev (DO NOT use in production):

      <domain-config cleartextTrafficPermitted="true">
          <domain includeSubdomains="false">10.0.2.2</domain>
          <domain includeSubdomains="false">localhost</domain>
          <domain includeSubdomains="false">127.0.0.1</domain>
      </domain-config>
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
</network-security-config>
